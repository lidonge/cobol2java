{{%isInnerClass=null;""}}
{{>dimstr}}
{{%dimension != null ? dim_push(dimension) : dim_push(0);""}}
{{>model.properties}}
{{%dim_pop();""}}
{{>model.pointer}}
{{%curObj=obj_getCurObj();""}}
{{^$IsPointer}}{{#$curObj}}
    {{^pictureClause}}
        {{^execSqlText}}
            {{%
                //println("=====in progProperties.mustache ");
            ""}}
            {{%isSubCopybook=obj_getVar("levelNumber") == 75 ? "isSubCopybook" :null;""}}
            {{>model.nopicture}}
            {{%
                //println("=====in progProperties.mustache innerCls is: "+innerCls + ", fieldName:"+ fieldName);
            ""}}
            {{%isSubCopybook = null;""}}
            {{>model.copy75}}
            {{%
                innerCls = isSubCopybook != null ? subCopyBookClsName : innerCls;
            ""}}
            private {{%innerCls)}} {{%fieldName}};
        {{/execSqlText}}
    {{/pictureClause}}
{{/curObj}}{{/IsPointer}}
{{#$IsPointer}}
    {{>model.nopicture}}
    private Object {{%fieldName}};
{{/IsPointer}}
