{{%isInnerClass=null;""}}
{{>dimstr}}
{{%dimension != null ? dim_push(dimension) : dim_push(0);""}}
{{>model.properties}}
{{%dim_pop();""}}
{{>model.pointer}}
{{%curObj=obj_getCurObj();""}}
{{^$IsPointer}}{{#$curObj}}
    {{^pictureClause}}
        {{^execSqlText}}
            {{%
                //println("=====in progProperties.mustache ");
            ""}}
            {{%isSubCopybook=obj_getVar("levelNumber") == 75 ? "isSubCopybook" :null;""}}
            {{>model.nopicture}}
            {{%isSubCopybook = null;""}}
            {{>model.copy75}}
            {{%
                innerCls = isSubCopybook != null ? subCopyBookClsName : innerCls;
                packName = model_getPackage(innerCls,"false");
                imports = obj_getFromMap(importsMap,mainClassName);
                packName !=null ?obj_addToList(imports,packName+"."+innerCls):"";
            ""}}
            private {{%innerCls)}} {{%fieldName}};
        {{/execSqlText}}
    {{/pictureClause}}
{{/curObj}}{{/IsPointer}}
{{#$IsPointer}}
    {{>model.nopicture}}
    private Object {{%fieldName}};
{{/IsPointer}}
